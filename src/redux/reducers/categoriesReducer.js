const initState = {
    counter: 0,
    cart_number: 0
}


export default function categoriesReducer(state = initState, action) {
    switch (action.type) {


        default:
            return state
    }
}
