const initState = {
    counter: 0,
    cart_number: 0
}


export default function cartReducer(state = initState, action) {
    switch (action.type) {


        default:
            return state
    }
}
